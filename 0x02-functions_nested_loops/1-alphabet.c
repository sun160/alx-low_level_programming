#include "main.h"

/**
 *
 * Description: print the alphabet, in lowercase, followed by a new line
 *
*/

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n');
}
